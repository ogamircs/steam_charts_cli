{
  "name": "steam-charts-cli",
  "version": "0.1.0",
  "description": "CLI for Steam current players, historical trends, forecasts, store metrics, and extrema.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "steam-charts": "./bin/steam-charts.js"
  },
  "scripts": {
    "test": "node --test",
    "lint": "node --check bin/steam-charts.js src/*.js test/*.js"
  },
  "dependencies": {
    "@bsull/augurs": "^0.10.2",
    "@bsull/augurs-prophet-wasmstan": "^0.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
